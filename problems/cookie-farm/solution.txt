NICE! here was my solution:
  
  var Kefir = require('kefir')
  var average = require('average')

  function deliciousness_est (a, b) {
    return (a*Math.LN2) * (3 / b)
  }

  module.exports = function (A_stream, B_stream) {
    var results = Kefir.combine([A_stream, B_stream], deliciousness_est)
    var every250ms = Kefir.interval(250, 1)
    return results.bufferBy(every250ms)
                  .map(average)
                  .map(Math.round)
  }


